import React from 'react';
import NavigationBar from '../../components/navigation-bar';

const Budget = () => {
    return (
        <NavigationBar>
            <div className='w-full h-full'>Budget</div>
        </NavigationBar>
    );
};

export default Budget;
